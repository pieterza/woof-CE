# Listen only on localhost
listen-address  0.0.0.0:8118

# Forward everything through Wingate with credentials
forward   /   http://CCS%5Coutbound:outbound@192.168.0.101:80/

# Optional but recommended – reduce logging noise
debug   0
